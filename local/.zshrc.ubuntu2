# Ubuntu machine 2-specific zsh configuration.

command_exists keychain && eval "$(keychain --eval --quiet --agents ssh id_rsa)"

path_append "/opt/nvim-linux-x86_64/bin"

source_if_exists "$HOME/.local/bin/env"
